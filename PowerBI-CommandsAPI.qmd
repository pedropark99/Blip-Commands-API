---
title: "Conectando o Power BI com o Blip Commands API"
author: "Pedro Duarte Faria"
format: revealjs
execute:
  echo: true
---

```{python configs}
#| include: false
exec(open("../../botKey.py").read())
exec(open("src/APICommands.py").read())
```



## Objetivo

Demonstrar como podemos conectar o Power BI ao Blip Commands API.


## O que é o Blip Commands API?

É uma API do Blip que disponibiliza dados de seu contato inteligente em **tempo real**.


## Consequência

Podemos utilizar o Commands API para servir aos nossos clientes os dados mais recentes. Ou seja, não precisamos **esperar o dia seguinte para termos acesso aos dados do dia atual**.


## Fluxo de dados

![](Figuras/PowerPoint/fluxo-dados.png)

## Temos uma biblioteca nova! 🥳

Para facilitar o acesso aos dados da Commands API, duas funções foram desenvolvidas para os APIN's:

- `get_event_track()`;
- `get_attendants_report()`;


## Importando as funções

Adicione o seguinte código ao seu notebook do Databricks:

```{verbatim}
%run /Library/APICommands
```

Mas antes de utilizar as funções, você precisa coletar a chave de autenticação de seu bot no Blip!

## Todo acesso precisa ser autenticado!

Todo acesso à Commands API precisa de uma chave de autenticação! Você encontra essa chave de autenticação na página de seu bot no Blip Portal. 

Basta acessar a área de "Configurações -> Informações de conexão", e, copiar o valor do quadro "Cabeçalho de autenticação".


## Todo acesso precisa ser autenticado!

![](Figuras/info-conexao.png)

## Chave de autenticação

Toda chave de autenticação é iniciada por `"Key "`, seguido de um conjunto aleatório de caracteres. Nos próximos slides, vou me refereir a essa chave através da variável `botKey`.

```{python}
#| eval: false
botKey = 'Key cq936dk_hxdd00_sa...'
```


## `get_event_track()`

Essa função possui os seguintes argumentos:

- `botKey`: a chave de autenticação;
- `tracking`: o nome do tracking;
- `startDate`: a data de início da pesquisa;
- `endDate`: a data de fim da pesquisa;
- `take` (**opcional**): quantidade máxima de resultados que você deseja trazer;
- `action` (**opcional**): a action específica que você deseja pesquisar;


## Exemplos:

```{python}
#| eval: true
from datetime import date
dia_pesquisa = date(2022,1,11)
r = get_event_track(
  botKey, tracking = "Saudacao sodexo selecao",
  startDate = dia_pesquisa, endDate = dia_pesquisa
)

print(r)
```




## Construindo uma requisição

Para acessar os recursos de uma API, você precisa enviar uma requisição HTTP para essa API. Essa requisição é em geral composta por três partes:

- Body;
- Header;
- QueryString;


## Construindo uma QueryString





